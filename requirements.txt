# Single source of truth for dependencies.
#
# Vercel looks for requirements.txt at the repo root to detect the framework
# and install the function's dependencies, so the real list lives here.
# backend/requirements.txt points at this file, which keeps the README's
# install command working unchanged.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
python-dotenv==1.0.1
httpx==0.28.1

# Document handling
pypdf==5.1.0
python-docx==1.1.2
