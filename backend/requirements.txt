# Dependencies live at the repo root so Vercel can find them; see
# ../requirements.txt. This indirection keeps `pip install -r
# backend/requirements.txt` working as the README documents.
-r ../requirements.txt
